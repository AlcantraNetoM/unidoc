<section class="space-y-6">
    <header>
        <h2 class="text-lg font-medium text-gray-900 dark:text-gray-100">
            {{ __('Delete Account') }}
        </h2>

        <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">
            {{ __('Once your account is deleted, all of its resources and data will be permanently deleted. Before deleting your account, please download any data or information that you wish to retain.') }}
        </p>
    </header>

    @if(auth()->user()->empresa_id && (auth()->user()->isGeneralTechnician() || auth()->user()->isNormalTechnician()))
        <!-- Mensagem para técnicos de conta empresarial -->
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
            <div class="flex">
                <div class="flex-shrink-0">
                    <svg class="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
                    </svg>
                </div>
                <div class="ml-3">
                    <h3 class="text-sm font-medium text-blue-800">
                        Não é possível eliminar a conta
                    </h3>
                    <div class="mt-2 text-sm text-blue-700">
                        <p>
                            Como usuário técnico de uma conta empresarial, você não pode eliminar sua própria conta. 
                            Para solicitar a remoção da sua conta, entre em contato com o administrador da empresa.
                        </p>
                        @if(auth()->user()->empresa && auth()->user()->empresa->getAdmin())
                            <p class="mt-2">
                                <strong>Administrador:</strong> {{ auth()->user()->empresa->getAdmin()->name }}
                                <br>
                                <strong>Email:</strong> {{ auth()->user()->empresa->getAdmin()->email }}
                            </p>
                        @endif
                    </div>
                </div>
            </div>
        </div>
    @else
        <!-- Botão normal de delete para outros tipos de usuário -->
        <x-danger-button
            x-data=""
            x-on:click.prevent="$dispatch('open-modal', 'confirm-user-deletion')"
        >{{ __('Delete Account') }}</x-danger-button>
    @endif

    @if(!(auth()->user()->empresa_id && (auth()->user()->isGeneralTechnician() || auth()->user()->isNormalTechnician())))
        <!-- Modal de confirmação (apenas para usuários que podem deletar a conta) -->
        <x-modal name="confirm-user-deletion" :show="$errors->userDeletion->isNotEmpty()" focusable>
            <form method="post" action="{{ route('profile.destroy') }}" class="p-6">
                @csrf
                @method('delete')

                <h2 class="text-lg font-medium text-gray-900 dark:text-gray-100">
                    {{ __('Are you sure you want to delete your account?') }}
                </h2>

                <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">
                    {{ __('Once your account is deleted, all of its resources and data will be permanently deleted. Please enter your password to confirm you would like to permanently delete your account.') }}
                </p>

                <div class="mt-6">
                    <x-input-label for="password" value="{{ __('Password') }}" class="sr-only" />

                    <x-text-input
                        id="password"
                        name="password"
                        type="password"
                        class="mt-1 block w-3/4"
                        placeholder="{{ __('Password') }}"
                    />

                    <x-input-error :messages="$errors->userDeletion->get('password')" class="mt-2" />
                </div>

                <div class="mt-6 flex justify-end">
                    <x-secondary-button x-on:click="$dispatch('close')">
                        {{ __('Cancel') }}
                    </x-secondary-button>

                    <x-danger-button class="ms-3">
                        {{ __('Delete Account') }}
                    </x-danger-button>
                </div>
            </form>
        </x-modal>
    @endif
</section>
